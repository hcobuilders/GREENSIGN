import { useMemo, useState } from "react";
type Project={id:string;code:string;name:string};type RecordRow={id:string;projectId:string|null;toolKey:string;type:string;state:string;revision:number;payload:unknown};
export function DataWorkspace({projects,records}:{projects:Project[];records:RecordRow[]}){const [query,setQuery]=useState("");const [project,setProject]=useState("all");const [type,setType]=useState("all");const sample=[{name:"Company qualification template",type:"template",projectId:null,source:"Proposal Designer",updated:"Today"},{name:"Master estimate workbook",type:"estimate",projectId:null,source:"Estimating",updated:"Yesterday"},{name:"Standard subcontract blocks",type:"template",projectId:null,source:"Commitments",updated:"Aug 06"},{name:"Submittal register export",type:"report",projectId:projects[0]?.id,source:"Submittals",updated:"Aug 05"}];const items=useMemo(()=>[...sample,...records.map(record=>{const payload=record.payload as {title?:string};return {name:payload?.title??record.type,type:record.type,projectId:record.projectId,source:record.toolKey,updated:`Revision ${record.revision??1}`};})].filter(item=>(project==="all"||item.projectId===project)&&(type==="all"||item.type.includes(type))&&`${item.name} ${item.source}`.toLowerCase().includes(query.toLowerCase())),[projects,records,project,type,query]);return <div className="library-workspace"><div className="filter-bar"><input className="search-input" value={query} onChange={e=>setQuery(e.target.value)} placeholder="Search files, reports, templates, budgets, estimates…"/><select value={project} onChange={e=>setProject(e.target.value)}><option value="all">All projects</option>{projects.map(item=><option value={item.id} key={item.id}>{item.code} · {item.name}</option>)}</select><select value={type} onChange={e=>setType(e.target.value)}><option value="all">All types</option><option value="file">Files</option><option value="report">Reports</option><option value="template">Templates</option><option value="estimate">Estimates</option><option value="submittal">Submittals</option></select><span className="result-count">{items.length} ITEMS</span></div><div className="library-grid">{items.map((item,index)=><article className="library-card" key={`${item.name}-${index}`}><span className="status-value">{item.type}</span><h3>{item.name}</h3><p>{item.projectId?projects.find(project=>project.id===item.projectId)?.name??"Project item":"Company library"}</p><footer><small>{item.source}</small><b>{item.updated}</b></footer><button className="outline-action">OPEN</button></article>)}</div></div>}
